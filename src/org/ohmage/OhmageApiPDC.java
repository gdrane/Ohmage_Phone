package org.ohmage;

import org.ohmage.OhmageApi.UploadResponse;

import edu.ucla.cens.pdc.libpdc.iApplication;
import edu.ucla.cens.pdc.libpdc.stream.DataStream;

public class OhmageApiPDC {
	
/*	public UploadResponse mobilityUpload(String serverUrl, 
			String username, String hashedPassword, 
			String client, String data)
	{
		// Ask the Server to pull data from the stream
		assert _app != null;
		
		DataStream ds = _app.getDataStream(id);
		ds.requestPull();
		return null;
	}
	private iApplication _app = null;
	*/
}
